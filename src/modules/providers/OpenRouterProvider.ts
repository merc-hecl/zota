/**
 * OpenRouterProvider - OpenRouter API implementation
 * Extends OpenAIProvider with OpenRouter-specific features
 * Currently inherits standard OpenAI-compatible behavior
 */

import { OpenAIProvider } from "./OpenAIProvider";

export class OpenRouterProvider extends OpenAIProvider {
  // OpenRouter inherits all OpenAI-compatible behavior
  // Future OpenRouter-specific features can be added here
}
