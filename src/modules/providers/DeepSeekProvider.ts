/**
 * DeepSeekProvider - DeepSeek AI API implementation
 * Extends OpenAIProvider with DeepSeek-specific features
 * Currently inherits standard OpenAI-compatible behavior
 */

import { OpenAIProvider } from "./OpenAIProvider";

export class DeepSeekProvider extends OpenAIProvider {
  // DeepSeek inherits all OpenAI-compatible behavior
  // Future DeepSeek-specific features can be added here
}
