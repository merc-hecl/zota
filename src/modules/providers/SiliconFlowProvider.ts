/**
 * SiliconFlowProvider - SiliconFlow API implementation
 * Extends OpenAIProvider with SiliconFlow-specific features
 * Supports both domestic (China) and international endpoints
 */

import { OpenAIProvider } from "./OpenAIProvider";

export class SiliconFlowProvider extends OpenAIProvider {
  // SiliconFlow inherits all OpenAI-compatible behavior
  // Future SiliconFlow-specific features can be added here
}
