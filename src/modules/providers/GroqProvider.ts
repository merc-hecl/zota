/**
 * GroqProvider - Groq Cloud API implementation
 * Extends OpenAIProvider with Groq-specific features
 * Currently inherits standard OpenAI-compatible behavior
 */

import { OpenAIProvider } from "./OpenAIProvider";

export class GroqProvider extends OpenAIProvider {
  // Groq inherits all OpenAI-compatible behavior
  // Future Groq-specific features can be added here
}
